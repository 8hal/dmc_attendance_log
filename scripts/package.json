{
  "name": "dmc-attendance-scripts",
  "version": "1.0.0",
  "description": "Migration and utility scripts",
  "scripts": {
    "migrate": "node migrate-sheets-to-firestore.js",
    "cleanup": "node cleanup-test-data.js",
    "cleanup:dry": "node cleanup-test-data.js --dry-run"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "googleapis": "^170.0.0"
  }
}
