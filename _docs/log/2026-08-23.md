# 2026-08-23 일지 — 회원 당일 UX v0.15.0 배포

## 요약

단체 대회 당일 QR·홈을 **지금 할 일 하나**로 정리한 Hosting 배포. 태그 `v0.15.0`. functions 변경 없음.

## 배포 목표

- 목표: 당일 회원이 QR·홈에서 지금 할 일 하나만 보게 한다.
- 범위: hosting만
- 성공: 프로덕션 `boarding.html`에 `이미 탑승됨` / `기록 확정하기`, 홈 CTA `기록 확인하기`
- 실패 시: Hosting을 `v0.14.0`으로 롤백

## 완료

- `bash scripts/pre-deploy-test.sh` — 72/72, `✅ 전체 통과 — 배포 가능`
- Firestore 프로덕션 백업: `backup/2026-08-23/` (gitignore)
- `main` fast-forward `52f162f7` → `a4206913` 후 `firebase deploy --only hosting`
- 태그 `v0.15.0` → `a4206913`

## 프로덕션 검증 (배포 직후 curl)

- `boarding.html` Last-Modified: `Sun, 23 Aug 2026 04:43:04 GMT`
- 문자열: `이미 탑승됨`, `기록 확정하기`, `assets/event-boarding-flow.js`
- `event-home-action.js` CTA: `기록 확인하기`, `boardingReturn` → `leg=return`
- JS SHA256 로컬 = 프로덕션 (`event-boarding-flow.js`, `event-home-action.js`)

## Git (main)

| 커밋 | 내용 |
|------|------|
| `39130aec` | feat: 당일 QR 탑승 확인과 홈 확정 우선 UX |
| `fa80602a` | fix: 닉 SSOT, 오는 편 CTA, 배번 판정 |
| `07323cda` | fix: 공유 폰과 완료 위계 |
| `e6106110` | copy: 확정 통일, 체크·컨펌 제거 |
| `a4206913` | docs: 배포 실행 주체를 AI로 변경 |

## 다음에 할 것

- 실제 대회 당일 QR(가는 편/오는 편)과 홈 카드 한 장 흐름을 현장에서 확인
- 공유 폰에서 이미 탑승 재스캔 → 다른 사람 선택이 먼저인지 확인

---

## v0.15.1 — 준비 단계 명단 쓰기 (functions + hosting)

버스 꺼짐은 회원 QR/총무 탑승 체크만 잠근다. CSV·지인·비고·제외는 준비 단계에서 가능.

- 목표: `1 준비`에서 파일 선택이 되고, 명단 수정이 된다
- 범위: functions + hosting
- 성공: 프로덕션 `event-admin.html`의 `csv-input`에 `ops-writable` 없음, 배너 `명단은 수정할 수 있습니다`
- 실패 시: functions·hosting을 `v0.15.0`으로 롤백

완료:

- `bash scripts/pre-deploy-test.sh` — 72/72
- Firestore 백업: `backup/2026-08-23/` (scrape_jobs 298, race_results 768, members 179)
- `main` fast-forward `976b1ea4` → `f4c248e8`
- `firebase deploy --only functions` 후 `hosting`
- 태그 `v0.15.1` → `f4c248e8`

프로덕션 검증:

- `event-admin.html` Last-Modified: `Sun, 23 Aug 2026 07:49:00 GMT`
- `csv-input`에 `ops-writable` 없음
- `removeRoster`/`saveNote`는 enabled 가드 없음, `toggleBoard`는 유지

---

## v0.15.2 — 탑승 인원 추가 문구 (hosting)

총무 버스 화면 메뉴 **지인 추가** → **탑승 인원 추가**. 명단 지인 배지는 그대로.

- 범위: hosting만
- 성공: 프로덕션 `event-admin.html`에 `탑승 인원 추가` 버튼, `>지인 추가<` 없음
- 실패 시: Hosting을 `v0.15.1`로 롤백

완료:

- `bash scripts/pre-deploy-test.sh` — 72/72
- `main` fast-forward `6ddb7293` → `dd03b913`
- `firebase deploy --only hosting`
- 태그 `v0.15.2` → `dd03b913`

프로덕션 검증:

- `event-admin.html` Last-Modified: `Sun, 23 Aug 2026 12:34:31 GMT`
- 제목·버튼 `탑승 인원 추가`

---

## v0.15.3 — 탑승 인원 추가 시 회원/지인 선택 (functions + hosting)

수동 추가 폼에 **구분** 셀렉트. 기본값 회원. 회원은 members에 닉이 없으면 거절하고, 지인은 `isGuest`로 넣는다. 명단 **지인** 배지는 그대로.

- 목표: 탑승 인원 추가 때 회원인지 지인인지 고를 수 있다
- 범위: functions + hosting
- 성공: 프로덕션 `event-admin.html`에 `#guest-kind`(회원/지인), `isGuest: kind === "guest"`
- 실패 시: functions·hosting을 `v0.15.2`로 롤백

완료:

- `bash scripts/pre-deploy-test.sh` — 72/72, `✅ 전체 통과 — 배포 가능`
- Firestore 백업: `backup/2026-08-23/` (scrape_jobs 298, race_results 768, members 179)
- `main` fast-forward `ef077ab5` → `29552842`
- `firebase deploy --only functions` 후 `hosting`
- 태그 `v0.15.3` → `29552842`

프로덕션 검증:

- `event-admin.html` Last-Modified: `Sun, 23 Aug 2026 13:52:43 GMT`
- SHA256 로컬 = 프로덕션
- `#guest-kind` 회원/지인, 버튼 `탑승 인원 추가`, `boarding-admin.html` `#guestKind` 동일
