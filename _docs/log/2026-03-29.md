# 2026-03-29 (일) 일일 로그

## 요약

**확정 API 회귀 수정**(`scrape_jobs.status`), **리포트 대회일·이탈 경고**, **races 하이라이트 제거**, **김포 중복·race_events 정리 스크립트**, **pre-deploy 에뮬레이터 전면 개선**(`emulators:exec` + Firestore 시드), 프로덕션 **백업**까지. 배포는 사용자가 직접 실행 예정(체크리스트·스킬 준수).

## 주요 작업

### Functions

- `confirm`: 기존 `scrape_jobs` 문서 `update` 시 **`status: "confirmed"`** 누락 보완 — 2026-03-23 `33e7d4c` 핫픽스에서 빠졌던 항목(완료 탭 미표시 원인).

### Hosting / 리포트 (`report.html`)

- 잡에 `eventDate` 없을 때 확정 화면에서 날짜 입력·API `eventDate` 전달.
- 확정 API 진행 중 `beforeunload`, 성공 토스트에 완료 탭 확인 안내.

### `races.html`

- 동마클 하이라이트 카드 제거(집계 부정확·UX).

### 데이터 정리 (프로덕션, 스크립트 실행)

- `scripts/fix-gimpo-empty-date-and-race-event.js`: `eventDate` 빈 `race_results` 삭제, `race_events` 생성·`canonicalEventId` 백필, `scrape_jobs` 메타 정리.

### 스크립트·테스트

- `pre-deploy-test.sh`: **`firebase emulators:exec`** + **functions, hosting, firestore** 동시 기동, 종료 시 자동 정리.
- `seed-emulator-pre-deploy.js`: 에뮬 Firestore에 최소 `members` 1건.
- `pre-deploy-test-runner.sh`: API·호스팅 검증 분리.
- 사전 조건: **`java`**, **`cd functions && npm ci`** (`firebase-functions` 없으면 에뮬 함수 로드 실패).
- delete-record 테스트 docId: `__test__` 제거(Firestore 예약 id로 에뮬 에러·지연 방지).

### 백업

- `backup/2026-03-29/` (스크립트 출력 기준: scrape_jobs 144, race_results 839, members 154 등 — 상세는 실행 로그).

## Git (당일 `main` 예시)

- `d359b29` pre-deploy 에뮬레이터
- `75536f7` confirm status + races 하이라이트 제거 + 김포 스크립트
- `f8446f7` report 이탈 경고·토스트
- `fa46734` report 대회일 입력
- (그 외 당일 병합·선행 커밋 다수 — `git log --since=2026-03-29` 참고)

## 다음에 할 것

- [ ] 로컬 `bash scripts/pre-deploy-test.sh` 통과 확인 후 **`firebase deploy --only functions`** → **`hosting`** (사용자 직접).
- [ ] 태그 `v0.9.4` 등 규칙에 맞게 생성·푸시.
- [ ] 배포 후 리포트 확정·완료 탭·대회별 기록 스모크.

## 수동 메모

- 퇴근 전 룰/스훅/스킬에 **에뮬 전제·리포트 vs races 데이터 소스** 보강 반영.
