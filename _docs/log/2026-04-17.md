# 2026-04-17 (금요일) 일일 로그

## 요약

출석 페이지 Phase 1 (MVP) 기획 및 설계 완료. PRD v2, Tech Spec, 구현 계획 작성. Subagent-Driven Development로 실행 준비 완료.

## 주요 작업

### 1. 출석 페이지 개편 기획 (Phase 1 MVP)

**배경**:
- 기존 출석 시스템: 매번 이름, 팀, 정모 타입 수동 입력
- 개선 목표: 원클릭 출석 + 게스트 지원 + 출석 통계

**브레인스토밍 (brainstorming skill)**:
- 3가지 디자인 옵션 제시 (A: Card Grid, B: Search, C: Dashboard)
- 비주얼 브레인스토밍 서버 활용 (브라우저 기반 모킹업)
- 사용자 피드백 반영: 하이브리드 접근
  - **C안 (Dashboard)**: 재방문자용 원클릭 출석
  - **B안 (Search)**: 첫 방문자용 환영 + 검색
  - **A안 (Volunteer Mode)**: Phase 2로 이동 (자봉 모드)

**주요 설계 결정**:
1. **멤버-출석 연결**: `memberId` 필드 추가 (Firestore document ID 기반)
2. **게스트 지원**: `isGuest` 필드 추가, 중복 체크 제외
3. **중복 체크**: `memberId` 우선, `nicknameKey` fallback
4. **팀 정보 관리**: 하이브리드 전략
   - 사용자: localStorage 즉시 업데이트
   - 운영자: 주기적으로 `members` 컬렉션 동기화
5. **베타 전략**: 별도 페이지 (`attendance-v2.html`)로 롤백 가능

### 2. 문서 작성

#### PRD v2 (`_docs/superpowers/specs/2026-04-17-attendance-page-redesign-v2.md`)
- Phase 1 범위 재정의 (C + B + Guest, A는 Phase 2)
- 상세 기능 요구사항
- 베타 테스트 전략

#### Tech Spec (`_docs/superpowers/specs/2026-04-17-attendance-page-tech-spec.md`)
- 시스템 아키텍처 (Single-page app, localStorage, Firebase API)
- 데이터 모델: `attendance` 스키마 확장 (`memberId`, `isGuest`)
- Firestore 인덱스 전략 (4개)
- API 설계: `POST /attendance` (중복 체크), `GET ?action=stats` (통계)
- 프론트엔드 아키텍처: `AppState`, 뷰 전환, 캐싱 (5분 TTL)
- 성능 최적화: 디바운싱, 인덱스, 캐싱
- 보안: XSS 방지, 입력 검증
- 롤백 전략: 프론트엔드 5분, 백엔드 3분

#### 구현 계획 (`_docs/superpowers/plans/2026-04-17-attendance-phase1-impl.md`)
- 13개 Task로 구성:
  1. Firestore 인덱스 생성 (4개)
  2-3. 백엔드 (중복 체크, 통계 API)
  4-9. 프론트엔드 (HTML, 상태관리, C안, B안, 게스트, 초기화)
  10. 통합 테스트
  11-12. 배포 (백엔드, 프론트엔드 베타)
  13. 베타 테스트 준비
- 예상 소요 시간: ~19시간
- TDD 기반, 단계별 커밋

### 3. 리뷰 프로세스

**Plan Review (plan-document-reviewer subagent)**:
- Critical Issues 발견:
  - `memberId` 기반 중복 체크 로직 개선 필요
  - 인덱스 생성 완료 확인 체크포인트 강화
- 모든 이슈 수정 후 재승인

### 4. 실행 준비

- **Subagent-Driven Development** 선택
- Todo 13개 생성 (Task 1-13)
- Task 1 (Firestore 인덱스 생성) in-progress로 전환
- Task 1은 수동 작업이므로 사용자 가이드 제공

## Git (당일 main 등)

```bash
# Commit 1: PRD v2 작성
33cd1d5 docs(plan): 출석 페이지 Phase 1 구현 계획 작성
```

**변경된 파일**:
- `_docs/superpowers/specs/2026-04-17-attendance-page-redesign-v2.md` (PRD)
- `_docs/superpowers/specs/2026-04-17-attendance-page-tech-spec.md` (Tech Spec)
- `_docs/superpowers/plans/2026-04-17-attendance-phase1-impl.md` (구현 계획)
- `/.superpowers/brainstorm/.../` (비주얼 모킹업 6개)

## 다음에 할 것

### Task 1: Firestore 인덱스 생성 (수동)
Firebase Console에서 4개 인덱스 생성:
1. `nicknameKey + meetingDateKey` (레거시 중복 체크)
2. `isGuest + nicknameKey + monthKey` (nickname 통계)
3. `memberId + meetingDateKey` (memberId 중복 체크) **← 중요!**
4. `isGuest + memberId + monthKey` (memberId 통계)

**⚠️ 모든 인덱스가 "Enabled" 상태 확인 후 다음 Task 진행**

### Task 2-13: Subagent-Driven Development
- 각 Task마다 fresh subagent 배포
- 2단계 리뷰: 스펙 준수 → 코드 품질
- 백엔드 → 프론트엔드 → 테스트 → 배포 순서

### 예상 일정
- 인덱스 생성: 10분
- 백엔드 (Task 2-3): 4시간
- 프론트엔드 (Task 4-9): 10시간
- 테스트 + 배포 (Task 10-13): 5시간
- **총 예상 시간: ~19시간**

## 수동 메모

### 주요 기술적 결정
1. **`memberId` 도입**: `members` 컬렉션 document ID를 `attendance`에 저장
   - 닉네임 변경 시에도 정확한 중복 체크
   - `nicknameKey`는 검색/표시용으로 병행 유지 (단계적 전환)

2. **하이브리드 팀 관리**:
   - 사용자: localStorage 업데이트 (즉시 반영)
   - 운영자: 주기적으로 `syncMemberTeams()` 실행
   - 출석 기록은 localStorage 스냅샷 저장

3. **Phase 1 범위 축소**:
   - Volunteer Mode (A안) Phase 2로 이동
   - MVP 집중: Dashboard + Search + Guest

### 설계 트레이드오프
- **인덱스 4개**: 저장 공간 증가 vs 쿼리 성능 (선택: 성능)
- **localStorage vs Firebase Auth**: 간단함 vs 보안 (선택: localStorage, Phase 2에서 Auth 고려)
- **별도 페이지 vs 기존 페이지 수정**: 롤백 vs 통합 (선택: 별도 페이지 `attendance-v2.html`)

### 참고 문서
- PRD: `_docs/superpowers/specs/2026-04-17-attendance-page-redesign-v2.md`
- Tech Spec: `_docs/superpowers/specs/2026-04-17-attendance-page-tech-spec.md`
- 구현 계획: `_docs/superpowers/plans/2026-04-17-attendance-phase1-impl.md`
