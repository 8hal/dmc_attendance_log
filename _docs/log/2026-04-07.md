# 2026-04-07 (화요일) 일일 로그

## 요약

단체 대회 파이프라인 구현 완료 (Tech Spec Task 1~7 전부). ops.html 오너 인증, group-events API 6개, Cloud Scheduler, ops.html 소스매핑, group.html 신규 페이지, QA 테스트 케이스 작성. PR #1 생성.

## 주요 작업

### 1. seed-emulator-pre-deploy.js 버그 수정 (main)
- `globalPaths.unshift` → `createRequire` 로 교체 (Node.js 24 + git worktree 환경에서 모듈 로딩 실패 문제 해결)
- main + feature 브랜치 양쪽 반영

### 2. 단체 대회 파이프라인 구현 (`feature/group-event-pipeline`)

**Task 1 — ops.html 오너 인증**
- `functions/.env`에 `DMC_OWNER_PW` 추가
- ops.html 로그인 오버레이, `tryAuth()`, `getOwnerPw()` 구현
- 오너 role만 진입 허용 (운영자 비밀번호로는 진입 불가)

**Task 2 — race_events 스키마 문서화**
- `data-dictionary.md`에 단체 대회 관련 8개 필드 추가

**Task 3 — group-events API 6개**
- GET 목록 / GET gap / POST promote / POST participants / POST source / POST scrape
- `triggerGroupScrape` 헬퍼: 기존 scrapeEvent 패턴 재사용
- 중복 스크랩 방지 (running 상태 체크), partial_failure 상태 반영

**Task 4 — Cloud Scheduler + Firestore 인덱스**
- `groupEventAutoScrape`: 매일 15:00 KST, 오늘 날짜 단체 대회 자동 스크랩
- `race_events` isGroupEvent + eventDate 복합 인덱스

**Task 5 — ops.html 소스매핑 섹션**
- 단체 대회 카드, 소스 6종 선택, 저장+즉시스크랩
- 오늘/내일 소스 미매핑 경고 배너

**Task 6 — group.html 신규 페이지**
- 고러닝 예정 대회 목록 → 단체 대회 등록
- 참가자 선택 모달 (all-members API)
- 갭 탐지 결과: ok/ambiguous/missing
- DNS/DNF 처리, 일괄 확정
- `confirm` 액션 `dnStatus` 지원 추가

**Task 7 — QA 테스트 케이스**
- 72개 케이스 작성 (`_docs/superpowers/specs/2026-04-07-group-event-pipeline-qa.md`)
- pre-deploy-test에 group.html 검증 3개 추가 → 42/42 통과

## Git (당일 main 등)

**main:**
- `22a03f0` fix(scripts): globalPaths → createRequire (Node.js 24 worktree 호환)

**feature/group-event-pipeline (PR #1):**
- `c0b8f16` fix(scripts): globalPaths → createRequire
- `9f1eb5e` feat(ops): 오너 전용 비밀번호 인증 추가
- `99d45f7` fix(ops): tryAuth 에러 핸들링 추가
- `46606f3` docs: race_events 단체 대회 파이프라인 필드 추가
- `0e9f91d` feat(api): group-events API 구현 (6개 액션 + triggerGroupScrape 헬퍼)
- `1bb1d6f` fix(api): group-events 중복 스크랩 방지 + partial_failure 상태 반영
- `e4b92a9` feat(scheduler): groupEventAutoScrape 추가 + race_events 복합 인덱스
- `92cca43` feat(ops): 단체 대회 소스매핑 섹션 추가
- `04d80af` fix(ops): group-events 응답 방어 코드 보강
- `97b61eb` feat: group.html 단체 대회 관리 페이지 + confirm dnStatus 지원
- `86f0ac5` fix(group): 프로덕션 API URL Cloud Run으로 수정
- `f31ca28` test: group.html 호스팅 검증 항목 추가
- `5af6a16` docs: 단체 대회 파이프라인 QA 테스트 케이스 작성

## 다음에 할 것

- QA 테스트 케이스 에뮬레이터에서 실행 (특히 GRP-16~23 갭 탐지 플로우)
- PR #1 리뷰 후 main 머지
- 배포 전 `functions/.env`에 `DMC_OWNER_PW` 프로덕션 값 설정 확인
- 배포 순서: firestore:indexes → functions → hosting

## 수동 메모

subagent-driven-development 스킬로 Task 단위 병렬 구현. worktree에서 seed 스크립트 버그(`globalPaths`)를 발견하여 main에도 함께 수정함. 서브에이전트가 main 브랜치에 직접 수정을 가한 사례가 있어 복사 후 main 롤백하는 방식으로 대응.
