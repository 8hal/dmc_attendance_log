# 2026-04-09 (목요일) 일일 로그

## 요약

- `my.html` 기록 검색 UX 개선 (최근 2주 대회 기본 표시, 월 선택 접기)
- `discover-all` / `ops-gorunning-events` API 서버 캐시 추가 (TTL 3시간)
- `ops.html` 캐시 수동 새로고침 기능 추가

## 주요 작업

### 1. 기록 검색 UX 개선 (`my.html`)
- **변경 전**: "기록 조회하기" 클릭 시 모든 월(16개월) 자동 선택 → 대회 목록 표시
- **변경 후**: 최근 2주 대회를 기본으로 즉시 표시 (내림차순)
  - 📅 최근 2주 대회 섹션 추가
  - 각 대회 카드에 날짜, 요일, 대회명 표시
  - "선택" 버튼으로 즉시 검색 가능
  - "▼ 월별로 검색하기" 접힌 토글 버튼
  - 월 선택 UI는 기존 기능 유지

### 2. API 서버 캐시 추가 (`functions/index.js`)
- **`discover-all` API**:
  - `search_cache` 컬렉션 사용
  - 캐시 키: `discover_all_{year}`
  - TTL: 3시간
  - `forceRefresh=true` 파라미터로 강제 갱신 지원
  
- **`ops-gorunning-events` API**:
  - `ops_meta/last_gorunning_crawl` 문서 사용
  - TTL: 3시간 (기존 6시간에서 단축)
  - `forceRefresh=true` 파라미터로 강제 갱신 지원
  - 캐시 HIT/MISS 로그 추가

### 3. ops.html 캐시 관리 기능
- "🔄 캐시 새로고침" 버튼 추가 (예정 대회 섹션)
- `refreshGorunningCache()` 함수 구현
  - 버튼 클릭 시 `forceRefresh=true`로 API 재호출
  - 로딩 상태 표시 ("⏳ 새로고침 중...")
  - 성공 시 알림 + 화면 갱신
- 캐시 상태 표시: "(캐시)" / "(실시간)" 구분

## Git (당일 main 등)

- 작업 브랜치: `main`
- 변경 파일:
  - `my.html`: 기록 검색 UX 개선
  - `functions/index.js`: 캐시 로직 추가
  - `ops.html`: 캐시 새로고침 버튼 추가
- **커밋 대기 중** (다음 주 배포 예정)

## 다음에 할 것

### 다음 주 배포 예정
- [ ] 로컬 에뮬레이터에서 최종 테스트
- [ ] 기록 검색 UX 사용자 피드백 확인
- [ ] 캐시 TTL (3시간) 적절성 검토
- [ ] 배포 전 체크리스트 실행 (`.cursor/skills/firebase-deploy/SKILL.md`)

### 추가 검토 필요
- my.html에도 "캐시 새로고침" 버튼 필요 여부 (사용자 vs 운영자)
- 캐시 무효화 필요 케이스 모니터링

## 수동 메모

- 에뮬레이터에서 정상 작동 확인 완료
- 서버 캐시 적용으로 응답 속도 10~20배 개선 (5~10초 → < 500ms)
- 하이브리드 캐시 전략 채택: 자동 캐시 (3시간) + 수동 갱신 기능
