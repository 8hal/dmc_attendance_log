# 2026-08-30 일지 — 회원 홈 프로필·버스 v0.16.0 배포

## 요약

회원 대회 홈을 프로필 카드 + 버스 카드로 바꾸고, 탭을 `홈 | 대회 기록`으로 정리한 functions+hosting 배포. 버스는 `openLeg`가 SSOT. 태그 `v0.16.0`.

## 배포 목표

- 목표: 회원 홈에서 배번·종목·확정과 편별 탑승을 한 화면에서 하게 한다
- 범위: functions + hosting
- 성공: 프로덕션 `event-home.html`이 `대회 기록` 탭과 `board=1`을 쓰고, `boarding.html`이 홈으로 `location.replace`하며, `bus-boarding` settings가 `enabled`만 있으면 `openLeg required`
- 실패 시: functions·hosting을 `v0.15.4`로 롤백

## 완료

- `bash scripts/pre-deploy-test.sh` — 82/82, `✅ 전체 통과 — 배포 가능`
- Firestore 프로덕션 백업: `backup/2026-08-30/` (scrape_jobs 301, race_results 770, members 179, search_cache 94911, member_search_jobs 173, event_logs 2819)
- `main` `a3b14d4c`에서 `firebase deploy --only functions` 후 `--only hosting`
- 태그 `v0.16.0` → `a3b14d4c`

## 프로덕션 검증 (배포 직후 curl)

- Last-Modified: `Sun, 30 Aug 2026 01:25:44 GMT` (`event-home.html`, `boarding.html`, `event-admin.html`, `event-roster.html`, `group.html`)
- SHA256 로컬 = 프로덕션 (위 5파일)
- `event-home.html`: `get("board") === "1"`, `대회 기록`, `명단·결과` 없음
- `boarding.html`: `event-home.html`, `board=1`, `location.replace`
- `event-admin.html`: QR `event-home.html` + `board=1`, `scrape-stop-btn`
- `GET /api/race?action=members` → `ok=true`, count 178
- `POST /api/race?action=bus-boarding` `{subAction:settings, enabled:true}` (가짜 eventId, 쓰기 없음) → HTTP 400 `openLeg required`
- `GET /api/race?action=event-logs&limit=20` → `ok=true`, 최근 항목에 `page_load` 포함

## Git (main, 기능 커밋)

| 커밋 | 내용 |
|------|------|
| `55670bc1` | feat(bus): openLeg SSOT, 옛 enabled만 있으면 꺼짐 |
| `7d6e730a` | feat(bus): settings/status/self-board가 openLeg를 쓴다 |
| `bf8bac45` | feat(event-admin): 편별 탑승 스위치, QR 하나, 총무 스크랩 |
| `e965fccb` | fix(tabs): 버스 탭 제거, 대회 기록 |
| `0bca6377` | feat(roster): 공개 대회 기록은 확정·DNS/DNF만 |
| `7d24d876` | feat(event-home): 프로필·버스 카드와 QR 탑승 랜딩 |
| `86eb430f` | feat(bib): update-bib가 canonical distance를 저장 |
| `34bdf2c8` | feat(confirm): 회원 PB와 수동 DNS/DNF/시각 |
| `db4c66f8` | feat(scrape): 총무 세션 시작 후 미완주만 재시도 |
| `767cae72` | fix(scrape): 세션 재시도는 기존 job에 bib merge |
| `83a1ec38` | feat(boarding): 옛 QR을 홈 탑승 랜딩으로 |
| `9e056e4c` | feat(event-admin): 총무 수동 DNS/DNF/시각 |
| `a3b14d4c` | fix(event-home): 대기 폴링은 네트워크 오류 뒤에도 유지 |

## 운영 주의

- 옛 `busBoarding.enabled: true`만 있고 `openLeg`가 없으면 **꺼진 것**으로 읽힌다. 배포 직후 총무가 지금 필요한 편을 다시 켠다.
- 철원(`2026-09-05`) 대회 **도중**에는 이 규칙을 다시 배포하지 않는다.

---

## v0.16.1 — event-admin 탑승 인원 시트·배번 문구 (hosting)

### 배포 목표

- 목표: 버스 탭 「탑승 인원 추가」를 하단 시트로 열고, 배번 없음 설명을 「스크랩 대상 아님」으로 바꾸며, 좁은 화면에서 배번 현황이 가로를 채우게 한다
- 범위: hosting만
- 성공: 프로덕션 `event-admin.html`에 `guest-open-btn`·`guest-sheet`·`없음 · 스크랩 대상 아님`·`align-items: stretch`, 로컬 SHA 일치
- 실패 시: hosting을 `v0.16.0`으로 롤백

### 완료

- `bash scripts/pre-deploy-test.sh` — 82/82
- Firestore 백업: `backup/2026-08-30/` (재실행, scrape_jobs 301 / race_results 770 / members 179 / search_cache 94911 / member_search_jobs 173 / event_logs 2819)
- `main` `c331f497`에서 `firebase deploy --only hosting`
- 태그 `v0.16.1` → `c331f497`

### 프로덕션 검증

- Last-Modified: `Sun, 30 Aug 2026 05:20:53 GMT` (`event-admin.html`)
- SHA256 로컬 = 프로덕션 (`event-admin.html`)
- `guest-open-btn`, `guest-sheet`, `스크랩 대상 아님`, `align-items: stretch` 존재 · `미참가로 봄` 없음
- `event-logs` `ok=true`, 최근 항목에 `page_load` 포함

### Git

| 커밋 | 내용 |
|------|------|
| `bfbddcac` | feat(event-admin): 탑승 인원 추가를 하단 시트로 연다 |
| `7375fa97` | test(event-admin): 하단 시트 닫기 경로를 고정한다 |
| `3b7ddc89` | fix(event-admin): 배번 없음 설명을 스크랩 대상 아님으로 바꾼다 |
| `b96904ef` | fix(event-admin): 좁은 화면에서 배번 현황이 가로로 채워지게 한다 |
| `c331f497` | merge: 배번 현황 문구·좁은 화면 레이아웃을 main에 합친다 |

---

## v0.16.2 — event-admin 명단 탑승 버튼·목록 스크롤 제거 (hosting)

### 배포 목표

- 목표: 버스 명단 행을 탑승/탑승 취소 버튼 + ⋯(비고·제외)로 바꾸고, 탑승 인원 추가는 목록 끝에 두며 명단 전용 스크롤을 없앤다
- 범위: hosting만
- 성공: 프로덕션 `event-admin.html`에 `roster-board-btn`·`roster-more`·`detachGuestOpenBtn`, `max-height: 50vh`·행 체크박스 없음, 로컬 SHA 일치
- 실패 시: hosting을 `v0.16.1`로 롤백

### 완료

- `bash scripts/pre-deploy-test.sh` — 82/82
- Firestore 백업: `backup/2026-08-30/` (재실행)
- `main` `8f1bcf43`에서 `firebase deploy --only hosting`
- 태그 `v0.16.2` → `8f1bcf43`

### 프로덕션 검증

- Last-Modified: `Sun, 30 Aug 2026 05:39:52 GMT` (`event-admin.html`)
- SHA256 로컬 = 프로덕션
- `roster-board-btn`, `roster-more`, `guest-open-btn`, `detachGuestOpenBtn` 존재 · `max-height: 50vh` 없음
- `event-logs` `ok=true`

### Git

| 커밋 | 내용 |
|------|------|
| `700b9bd8` | fix(event-admin): 명단 탑승을 버튼으로, 비고·제외는 더보기 |
| `c0a1205a` | Merge remote-tracking branch roster-board-btn |
| `8f1bcf43` | fix(event-admin): 명단 탑승 버튼 머지, 추가 버튼을 목록 끝에 둔다 |

## v0.16.3 배포 (hosting)

- 범위: hosting only (functions 변경 없음)
- 내용: 버스 명단 필터, 탑승 QR 단일화, 프로필 대기 카드 문구, 춘백 D-day 색
- pre-deploy: 83/83 통과
- 백업: `backup/2026-08-30/`
- 태그: `v0.16.3` @ `593e8a06`

## v0.16.4 배포 (hosting)

- 범위: hosting only
- 내용: 회원 홈 대기 카드 `.bib-face` (배번 페이스)
- pre-deploy: 83/83 통과
- 백업: `backup/2026-08-30/`
- 태그: `v0.16.4` @ `357ae94c`

## v0.16.5 배포 (hosting)

- 배번 페이스: 핀홀 제거, `eventName`/`primaryName` 대회명, 링크 한 줄
- pre-deploy 83/83, 태그 `v0.16.5`

## v0.16.6 배포 (hosting)

- 대기 카드 prompt: 대회 종료 후 자동 수집·확정 안내
- 태그 `v0.16.6`

## v0.16.7 배포 (hosting)

- 기록 직접 입력: 「이번이 PB예요」를 완주 시각 오른쪽 한 줄
- 태그 `v0.16.7`

## v0.16.8 배포 (hosting)

- 기록 직접 입력: 완주 시각 시·분·초 분리 (`EventFinishTime`)
- 태그 `v0.16.8`

## v0.16.9 배포 (hosting)

### 배포 목표

- 목표: 기록 직접 입력에서 DNS·DNF를 한 가로 줄로 두고 저장 버튼 위 여백을 늘리며, 「돌아가기」로 대기 카드로 복귀
- 범위: hosting만
- 성공: 프로덕션 `event-home.html`에 `profile-dn-row`·`profileManualBack`, CSS에 `#profileManualSave { margin-top }`, 로컬 SHA 일치
- 실패 시: hosting을 `v0.16.8`로 롤백

### 완료

- `bash scripts/pre-deploy-test.sh` — 83/83, `✅ 전체 통과 — 배포 가능`
- Firestore 백업: `backup/2026-08-30/` (재실행, scrape_jobs 303 / race_results 770 / members 179 / search_cache 94911 / member_search_jobs 173 / event_logs 2820)
- `main` `0f6e3d7b`에서 `firebase deploy --only hosting`
- 태그 `v0.16.9` → `0f6e3d7b`

### 프로덕션 검증

- Last-Modified: `Sun, 30 Aug 2026 07:00:41 GMT` (`event-home.html`, `assets/event-member-shell.css`)
- SHA256 로컬 = 프로덕션 (위 2파일)
- `profile-dn-row` + `profileDnsBtn`/`profileDnfBtn` 가로 배치, `#profileManualSave { margin-top: 20px }`, `profileManualBack`「돌아가기」
- unit: `event-home-manual-dn-row.test.js` 3/3, `npm run test:event-home` 122/122
- `event-logs` `ok=true`

## v0.16.10 배포 (hosting)

### 배포 목표

- 목표: 배번 프로필 입력 안내를 「대회 기록 자동 수집을 위해 배번과 종목을 입력해주세요.」로 통일
- 범위: hosting만 (`assets/event-home-action.js` copy)
- 성공: 프로덕션 `assets/event-home-action.js`에 새 prompt 2곳, 구 「배번과 종목을 넣어 주세요.」 없음, 로컬 SHA 일치
- 실패 시: hosting을 `v0.16.9`로 롤백

### 완료

- `bash scripts/pre-deploy-test.sh` — 83/83, `✅ 전체 통과 — 배포 가능`
- Firestore 백업: `backup/2026-08-30/` (재실행, scrape_jobs 303 / race_results 770 / members 179 / search_cache 94911 / member_search_jobs 173 / event_logs 2820)
- `main` `4c4c461b`에서 `firebase deploy --only hosting`
- 태그 `v0.16.10` → `4c4c461b`

### 프로덕션 검증

- Last-Modified: `Sun, 30 Aug 2026 07:21:28 GMT` (`assets/event-home-action.js`)
- SHA256 로컬 = 프로덕션 (`712be6c6…`)
- prompt 2곳: 「대회 기록 자동 수집을 위해 배번과 종목을 입력해주세요.」
- 구 문구 「배번과 종목을 넣어 주세요.」 없음
- unit: `event-home-action.test.js` 19/19

## v0.16.11 배포 (hosting)

### 배포 목표

- 목표: 버스 카드 제목을「가는/오는 버스」대신 `동탄 → {dest}` / `{dest} → 동탄`(완료 시 `· 탑승 완료`)로 표시
- 범위: hosting만 (`event-home.html`, `assets/event-home-action.js`, `assets/event-member-copy.js`)
- 성공: 프로덕션에 `busRouteTitle`·`busDestinationLabel` 존재, 구「가는 버스 탑승 완료」/「오는 버스 탑승 완료」없음, 로컬 SHA 일치
- 실패 시: hosting을 `v0.16.10`으로 롤백

### 완료

- `bash scripts/pre-deploy-test.sh` — 83/83, `✅ 전체 통과 — 배포 가능`
- Firestore 백업: `backup/2026-08-30/` (재실행, scrape_jobs 303 / race_results 770 / members 179 / search_cache 94911 / member_search_jobs 173 / event_logs 2820)
- `main` `44112f19`에서 `firebase deploy --only hosting`
- 태그 `v0.16.11` → `44112f19`

### 프로덕션 검증

- Last-Modified: `Sun, 30 Aug 2026 07:23:54 GMT` (`event-home.html`, `assets/event-home-action.js`, `assets/event-member-copy.js`)
- SHA256 로컬 = 프로덕션 (위 3파일)
- `EventHomeAction.busRouteTitle` 5회, `busDestinationLabel` 2회, 구「가는/오는 버스 탑승 완료」0
- unit: `npm run test:event-home` 133/133
- helper: `동탄 → 철원` / `철원 → 동탄 · 탑승 완료`
- `event-logs` `ok=true`, 배치에 `page_load` 포함

### Git

| 커밋 | 내용 |
|------|------|
| `44112f19` | feat(event-home): 버스 카드 제목을 동탄↔목적지 경로로 표시 |

---

## v0.16.12 배포 (functions + hosting)

### 배포 목표

- 목표: 총무가 `placeClub`/`placeVenue`(예: 동탄/철원)를 저장하면 회원 홈 버스 카드가 긴 대회명 대신 `동탄 → 철원 · 탑승 완료`를 쓴다
- 범위: functions + hosting (`bus-boarding` settings 확장, `event-admin.html`, `event-home.html`, helpers)
- 성공: 프로덕션에 `place-club-input`·`busClubLabel`·`parseSettingsPlaces`, 로컬 SHA 일치, `enabled:true`만 보내면 여전히 `openLeg required`
- 실패 시: functions·hosting을 `v0.16.11`로 롤백

### 완료

- `bash scripts/pre-deploy-test.sh` — 83/83, `✅ 전체 통과 — 배포 가능`
- Firestore 백업: `backup/2026-08-30/` (재실행, scrape_jobs 303 / race_results 770 / members 179 / search_cache 94911 / member_search_jobs 173 / event_logs 2820)
- 브랜치 `cursor/bus-place-labels-d30f` `04d2aad6`에서 `firebase deploy --only functions` 후 `--only hosting`
- 태그 `v0.16.12` → `04d2aad6`

### 프로덕션 검증

- Last-Modified: `Sun, 30 Aug 2026 07:32:41 GMT` (`event-admin.html`, `event-home.html`, `assets/event-home-action.js`, `assets/event-member-copy.js`)
- SHA256 로컬 = 프로덕션 (위 4파일)
- `event-admin.html`: `place-club-input`·`place-venue-input`·`save-place-labels-btn`·`savePlaceLabels`
- `event-home.html`: `busClubLabel`·`placeClub:`
- helper: `동탄 → 철원 · 탑승 완료` / `철원 → 동탄`
- `GET members` → `ok=true`, count 178
- `POST bus-boarding settings {enabled:true}` (쓰기 없음) → HTTP 응답 `openLeg required`
- `event-logs` `ok=true`, 최근 배치에 클라이언트 이벤트 포함

### Git

| 커밋 | 내용 |
|------|------|
| `2d32d446` | docs: 버스 카드 placeClub/placeVenue 설계와 데이터 사전 |
| `04d2aad6` | feat(bus): 총무 placeClub/placeVenue로 버스 카드 경로 제목 |

---

## v0.16.13 배포 (hosting)

### 배포 목표

- 목표: (1) 장소 입력이 폴링에 안 지워지고 event-admin은 버스 탭만 폴링 (2) 오는 편 탑승 버튼이 회원 홈에 표시
- 범위: hosting만 (`functions/` 변경 없음)
- 성공: 준비 탭 장소 입력 유지; 버스 탭만 주기 갱신; 총무 「오는 켜기」 후 회원 `#busCard`에 탑승하기
- 실패 시: hosting을 `v0.16.12`로 롤백

### 완료

- `bash scripts/pre-deploy-test.sh` — 83/83, `✅ 전체 통과 — 배포 가능`
- Firestore 백업: `backup/2026-08-30/` (재실행, scrape_jobs 303 / race_results 770 / members 179 / search_cache 94911 / member_search_jobs 173 / event_logs 2820)
- `main`에 `cursor/place-labels-poll-fix-d30f` + `cursor/return-bus-cta-d30f` 머지
- 초기 hosting 업로드에서 `content hash doesn't match` → `firebase.json` hosting ignore에 `*.log` 추가(`a2768140`) 후 `firebase deploy --only hosting` 성공
- 태그 `v0.16.13` → `c6bd8088` (기능 머지). ignore 수정은 태그 이후 `a2768140`

### 프로덕션 검증

- Last-Modified: `Sun, 30 Aug 2026 08:06:00 GMT` (`event-admin.html`, `event-home.html`, `assets/event-admin-place-labels.js`)
- SHA256 로컬 = 프로덕션 (위 3파일)
- `event-admin.html`: `shouldPollBusStatus`, `event-admin-place-labels.js`, `place-club-input`, `savePlaceLabels`
- `assets/event-admin-place-labels.js`: `shouldSyncPlaceLabels`·`shouldPollBusStatus`·`shouldApplyPlaceFromServer`
- `event-home.html`: `#busCard`, `busBoardBtn`「탑승하기」, `openLeg` reload 후 `renderBusCard`
- unit: `npm run test:event-admin` 91/91, `npm run test:event-home` 145/145
- `GET members` → `ok=true`
- `event-logs` `ok=true`, 배치에 `page_load` 포함

### Git

| 커밋 | 내용 |
|------|------|
| `07cd28ba` | fix(event-admin): 장소 라벨 입력이 폴링에 지워지지 않도록 보호 |
| `47b25e10` | fix(event-admin): 4초 폴링을 버스 섹션으로 한정 |
| `2405943c` | fix(event-home): 오는 편 openLeg 반영해 버스 탑승 버튼 표시 |
| `c6bd8088` | Merge branch `cursor/return-bus-cta-d30f` |
| `a2768140` | fix(hosting): 배포 중 갱신되는 `*.log`를 ignore에 추가 |
