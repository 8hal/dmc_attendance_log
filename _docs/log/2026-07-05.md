# 2026-07-05 (Sunday) 일일 로그

## 요약

**6/30 정회원 명단(176명) 프로덕션 반영** 및 **퇴회·제명 익명화** functions 배포·검증 완료. 저녁 **`main` hosting 배포가 키오스크 UI를 덮어쓴 회귀** 발생 → `integrate/kiosk-and-member-leave`(키오스크 + main 통합)에서 **functions·hosting 재배포**로 복구.

## 주요 작업

### 정책·구현 (PR #4 → main)
- `_docs/superpowers/policies/member-leave-anonymization-policy.md` — 탈퇴=제명 동일, `hidden`+익명화, 연관 컬렉션 갱신
- `functions/lib/member-leave.js` — `applyMemberLeave()` 공통 모듈
- `hide-member` API — 익명화 + `leaveReason`/`leftAt` 지원
- `update-member` — 익명화된 회원 `hidden: false` 복원 차단
- `admin.html` — 퇴회 UI, `_archived*`·제명 표시

### 명단 동기화 (API 우선)
- `bulk-sync-members` 신규 API **생략** (~20건 → `add-member` / `update-member` / `hide-member` 반복)
- `scripts/plan-members-sync.js`, `scripts/apply-members-sync-via-api.js`
- 스킬: `members-sync-via-api` (적용), `members-sync-via-mcp` (baseline 조회)

### 프로덕션 적용 (6/30 vs MCP baseline 171명 → 8건 API)
| 구분 | 건수 | 내용 |
|------|------|------|
| 신규 | 6 | 김철응(빵야), 이봉건(Clint), 구본진(덕구아빠), 이정화(차차), 김원겸(지구인), 김태우(까치식당) |
| 닉 변경 | 1 | 강동원: 해피하우스 → 하우스 |
| 퇴회(제명) | 1 | 이경주(초이스), `leaveReason: expelled` |

※ functions 배포 **후** `hide-member` 재호출로 익명화 완료 (attendance 13, race_results 14건).

### 키오스크 hosting 회귀·복구
| 시각 | 사건 |
|------|------|
| 오후 | `main` 기준 `firebase deploy --only hosting` → 프로덕션에서 **「현장 출석 모드」**·`?mode=kiosk` 사라짐 (실서버는 `cursor/attendance-kiosk-blocker2-e814` 기준이었음) |
| 저녁 | `integrate/kiosk-and-member-leave` = kiosk 브랜치 + `main` **충돌 없이 머지** (`d6e728a`) |
| 저녁 | 통합 브랜치에서 **functions + hosting** 재배포 → 키오스크 UI 복구 확인 |

프로덕션 검증 (hosting 재배포 후):
- `attendance-v2.html` 내 `현장 출석 모드` 링크 2곳
- `attendance-v2.js` 키오스크 로직 (`openKioskView` 등)
- `assets/design-tokens.css` 200

### 배포 타임라인
| # | 브랜치 | 대상 | 결과 |
|---|--------|------|------|
| 1 | `main` | functions | ✅ hide-member 익명화 |
| 2 | `main` | hosting | ✅ (이후 키오스크 UI 회귀 유발) |
| 3 | `integrate/kiosk-and-member-leave` | functions | ✅ 키오스크 출석 로깅 등 포함 |
| 4 | `integrate/kiosk-and-member-leave` | hosting | ✅ 키오스크 UI 복구 |

## Git (당일)

| 브랜치/태그 | 내용 |
|-------------|------|
| `main` `057897f` | 일지·명단 sync·익명화 머지 완료 |
| `integrate/kiosk-and-member-leave` `d6e728a` | kiosk + main merge |
| `integrate/kiosk-and-member-leave` `4a18cac` | 일지 보강 |
| `v0.13.1` | hide-member 익명화 + 6/30 명단 sync |
| PR #4 | `gh pr merge` 권한 부족 — `main` push로 내용 반영, PR Close 대기 |

## 다음에 할 것

- [ ] `integrate/kiosk-and-member-leave` → **`main` 머지·푸시** (프로덕션 배포 브랜치와 git SSOT 일치)
- [ ] GitHub PR #4 Close
- [ ] 현장 키오스크 스모크: 신규 닉 검색·`?mode=kiosk` 첫 글자 그리드
- [ ] `race_results` 레거시 `memberNickname` 필드 UI 영향 확인
- [ ] `git config pull.ff only` (divergent branches hint 방지)

## 수동 메모

- **교훈:** `hosting.public`이 `.`이면 배포 디스크 = Git HEAD가 아님. **키오스크·main 분기 시 hosting만 main으로 올리면 현장 UI 회귀.**
- functions 배포 전 `hide-member`는 `hidden`만 → 익명화는 **신버전 functions 배포 후 재호출** 필수
- `pre-deploy-test.sh`는 8080 포트 점유 시 실패
- 키오스크 URL: `attendance-v2.html?mode=kiosk` (정모일·유형은 쿼리 또는 화면에서 고정)
