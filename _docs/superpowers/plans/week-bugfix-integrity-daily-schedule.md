# 주간 루틴 (요일별) — UX 버그 + 집행 층 + data-integrity

> 상위 플랜: Cursor `주간_버그·무결성_플랜_b5563003.plan.md`  
> 체크리스트 템플릿: [`_docs/log/2026-03-30-week-ux-checklist.md`](../log/2026-03-30-week-ux-checklist.md) (복사해 주차별로 이름 변경 가능)  
> **한 주 기본:** 월~금 근무 가정. **필수 MVP**는 플랜 문서 기준(체크리스트·집행 층 1p·`data-integrity` 스냅샷 1건).

---

## 월요일 — 방향 잡기

| 담당 | 할 일 |
|------|--------|
| **사람** | 이번 주 “P0 UX 후보”가 있으면 메모(없어도 됨). 배포·데이터 작업 일정 유무 확인. |
| **함께** | 주간 목표 3줄만 확정: (1) 체크리스트 어디까지 채울지 (2) `data-integrity` 돌릴 날 (3) 코드 수정 0~2건 여부. |
| **에이전트** | 지난 주 백로그·`_docs/log` 최신 파일 훑고, 이번 주 체크리스트 파일명(예: `YYYY-MM-DD-week-ux-checklist.md`) 제안. |

---

## 화요일 — 축 A (개인) + 축 C 점검

| 담당 | 할 일 |
|------|--------|
| **사람** | **my.html** 체크리스트 행 채우기(기대/실제/결과). 30~45분이면 1차 완료 가능. |
| **사람** | (분기) 집행 층 표가 오래됐으면 “운영 스크립트 1개 추가됐는지”만 스캔 후 갭 문서에 한 줄 추가. |
| **에이전트** | 체크리스트에서 **실패**로 표시된 항목만 읽고, 코드 경로·재현 가설 정리(수정은 아직 선택). |

---

## 수요일 — 축 A (운영) + 축 B

| 담당 | 할 일 |
|------|--------|
| **사람** | **report.html** 체크리스트 채우기(`verify-admin` → 스크랩 → 검토 → `confirm` 흐름). |
| **사람** | **`data-integrity` 스냅샷** 1회: 브라우저 또는 `curl`로 production race API `?action=data-integrity` → JSON을 `_docs/log/YYYY-MM-DD-data-integrity.json`에 저장(날짜·환경 주석 또는 래퍼 필드). |
| **에이전트** | 스냅샷 파일을 붙여 넣어 달라고 하면 래퍼 JSON 생성·`issues.length` 요약 문장 작성. |

---

## 목요일 — 축 A (races) + 스트레치 시작

| 담당 | 할 일 |
|------|--------|
| **사람** | **races.html** 체크리스트 채우기. |
| **사람** | P0 **1건** 선정 → 재현 스텝을 이슈/슬랙/채팅에 한 블록으로 적기. |
| **에이전트** | P0 1건에 대해 **최소 수정** 패치 + `bash scripts/pre-deploy-test.sh` (사용자 요청 시). |

---

## 금요일 — 마감 + 백로그

| 담당 | 할 일 |
|------|--------|
| **사람** | P0 **2번째**가 있으면 동일(재현 → 에이전트 패치 요청 또는 다음 주로 미룸). **최대 2건** 초과는 백로그만. |
| **사람** | 금요일 산출물 정리: (1) 채워진 체크리스트 경로 (2) 이번 주 `data-integrity` 파일명 (3) 코드 수정 있으면 커밋 요약 한 줄 (4) 다음 주 백로그 1페이지. |
| **사람** | (배포 주간이면) `pre-deploy-test` → 백업 → **직접** `firebase deploy` — 에이전트는 명령어만 안내. |
| **에이전트** | `audit-*` dry-run 스크립트나 감사 쿼리 초안 — **시간 남을 때만** 요청 시 수행. |

---

## 매일 10분 (선택)

| 담당 | 할 일 |
|------|--------|
| **사람** | 프로덕션에서 한 번이라도 이상하면 `_docs/log/YYYY-MM-DD.md`에 한 줄. |
| **에이전트** | 해당 로그·관련 HTML/함수만 짚어 원인 후보 나열(데이터 쓰기는 승인 전 금지). |

---

## 이미 끝난 것 (2026-03-30 실행분)

- 축 C 초안: [`2026-03-30-enforcement-layers-gaps.md`](../log/2026-03-30-enforcement-layers-gaps.md)
- 축 B 스냅샷: [`2026-03-30-data-integrity.json`](../log/2026-03-30-data-integrity.json)
- 축 A 템플릿: [`2026-03-30-week-ux-checklist.md`](../log/2026-03-30-week-ux-checklist.md) — **사람이 행 채우기만 남음**

이후 주차는 같은 요일 루틴을 반복하고, 파일명만 날짜를 바꿔 저장하면 된다.
